[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: marks tests that require demucs/torch and run a real separation (deselect with '-m "not integration"')
